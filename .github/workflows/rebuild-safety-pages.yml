# Rebuilds the /safety/ programmatic pages monthly so they track the crime-data
# cadence (NYPD YTD refreshes roughly quarterly; a monthly rebuild also keeps
# lastmod honest for crawlers). Safe against prod: generate-data.mjs paces
# itself (1 concurrent, 1.2s delay) under the backend's 60/min rate limit, and
# retries absorb a free-dyno cold start.
#
# REQUIRED SECRET (Settings → Secrets and variables → Actions):
#   SAFEROUTE_API_KEY — same shared secret the app uses.
name: rebuild-safety-pages

on:
  schedule:
    - cron: '17 9 2 * *'   # 2nd of each month, 09:17 UTC
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    # Explicit, because the default is 6 hours. Sized from measured runs:
    #   run #4  2026-09-02  10 cities  1,222 areas   58m33s data step
    #   run #6  2026-09-11  20 cities  1,917 areas  118m20s
    #   run #7  2026-09-11  22 cities  2,050 areas  133m41s
    # That is a steady ~3.5 s/area against the free-tier backend — nearly 3x
    # the 1.2s GEN_DELAY_MS, because the backend's own fetch dominates. Sizing
    # this from the pacing constant (as it was originally) under-read it by
    # 2.4x. Rule of thumb: ~6 min per city added.
    # 240 leaves roughly 1.6x over run #7 and room for Bristol and Cardiff.
    # The cap is cheap: the push is the last step, so a killed run publishes
    # nothing and the next run redoes the work. Re-measure past ~30 cities.
    timeout-minutes: 240
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22

      # ONE list, derived, used by all three steps below.
      #
      # These steps used to repeat the city list by hand and it drifted three
      # times in two days: the nine cities up to cleveland (704 pages), then
      # kansascity (244), then birmingham and liverpool (133). Every drift was
      # silent — a missing city keeps re-rendering from whatever crime data was
      # committed when it was built, while its pages still print a current
      # "data through <date>" line. A comment saying "remember to add it here
      # too" was already in this file when the last two drifted past it.
      #
      # safety/<city>/ is the source of truth because it IS the published
      # corpus: a city has pages iff it has a directory, so the list cannot
      # disagree with what the site serves. (Deriving it from render-pages.mjs
      # CITIES would couple these steps to a file that is edited constantly.)
      # Written to a file, not a step output: a step output would be pasted in
      # by ${{ }} before bash sees it, and a multi-line value pasted into a
      # `for c in ...` ends the statement at the first newline and then tries to
      # RUN the second city as a command.
      - name: Work out which cities are published
        run: |
          ls -d safety/*/ | xargs -n1 basename | grep -vxE 'assets|data' | sort > /tmp/cities.txt
          echo "$(wc -l < /tmp/cities.txt | tr -d ' ') published cities:"
          tr '\n' ' ' < /tmp/cities.txt; echo

      - name: Refresh gazetteers (boundaries rarely change; cheap)
        # build-gazetteer-la preserves committed density-snapped centroids, so
        # this does not undo snap-la-centroids. The snap step itself is not run
        # monthly — boundaries are stable and the snapped points are committed.
        #
        # Unknown city => the loop FAILS here, in the first minute, rather than
        # letting a city silently go unrefreshed for months. If you are reading
        # this because the job failed: give the new city a per-city builder, or
        # add it to build-gazetteer-ukcity.mjs, or add it to SKIP with a reason.
        run: |
          # Committed gazetteers that must NOT be rebuilt. Their only builder is
          # build-gazetteer-calibration.mjs, which emits calibration-grade sets:
          #   * It never emits `borough`. render-pages.mjs groups the city hub by
          #     that field and calls b.toLowerCase() on it, so a rebuilt gazetteer
          #     crashes the render step. The field was hand-backfilled into these
          #     three in 05a8cb25 for exactly that reason.
          #   * longbeach would come back as all 123 boundary areas. The published
          #     set is the curated 96: 73f0dbe6 dropped 25 "Unassigned" marina,
          #     park and power-station fragments plus a bare "AOC7" code and a
          #     duplicate, and the backend normaliser was refitted (241 -> 326) for
          #     the 96. Restoring the artifacts would re-add thin pages AND leave
          #     the city scored by a normaliser fitted to a different area set.
          # Worse, run with NO args it rebuilds every calibration city, which
          # includes baltimore — replacing its published 56 CSAs with 256 NSAs.
          # If one of these ever needs rebuilding, give it its own per-city
          # builder (as baltimore has); do not reach for the calibration script.
          SKIP="denver longbeach vancouver"
          for c in $(cat /tmp/cities.txt); do
            case " $SKIP " in *" $c "*) echo "skip $c (committed gazetteer)"; continue ;; esac
            # The one city whose page slug and builder name differ: pages live
            # at /safety/new-york/ but the builder is build-gazetteer-nyc.mjs.
            case "$c" in new-york) b=nyc ;; *) b=$c ;; esac
            if [ -f "tools/build-gazetteer-$b.mjs" ]; then
              node "tools/build-gazetteer-$b.mjs"
            elif grep -qE "^  $c: *\{" tools/build-gazetteer-ukcity.mjs; then
              node tools/build-gazetteer-ukcity.mjs --city "$c"
            else
              echo "::error::$c is published but has no gazetteer builder and is not in SKIP"
              exit 1
            fi
          done

      - name: Refresh safety data (paced against prod backend)
        env:
          SAFEROUTE_API_KEY: ${{ secrets.SAFEROUTE_API_KEY }}
          SAFEROUTE_BASE_URL: https://saferoute-backend-w5yf.onrender.com
        run: |
          for c in $(cat /tmp/cities.txt); do
            node tools/generate-data.mjs --force --city "$c"
          done

      # Incremental — cached areas are skipped, so a normal run fetches nothing
      # and never touches Overpass. Only genuinely new areas cost a request.
      # NYC has its own basemap source; every other city is OSM via the gazetteer.
      - name: Refresh basemaps
        run: |
          node tools/fetch-basemap.mjs
          for c in $(cat /tmp/cities.txt); do
            [ "$c" = "new-york" ] && continue
            node tools/fetch-basemap-osm.mjs --city "$c"
          done

      # The homepage states "Requires iOS X+" by hand, and it went stale the day
      # 1.2.3 dropped the floor from 26.4 to 16.4 — telling older iPhones they
      # could not install an app they could. This copies the App Store's own
      # answer; the commit step below picks up any correction. It never fails
      # the run (an unreachable lookup is not a content error).
      - name: Keep App Store facts on the site true
        run: node tools/check-app-facts.mjs

      # The transparency dataset is MEASURED in the backend repo (the providers
      # live there) and pushed here by its monthly-data-audit job, which runs on
      # the 1st so the data is waiting by the time this runs on the 2nd.
      # Guarded: a missing file skips the page rather than failing a 25-minute
      # rebuild of 1,222 pages over one auxiliary page.
      - name: Render the transparency page
        run: |
          if [ -f tools/data/transparency-index.json ]; then
            node tools/render-transparency.mjs || echo "::warning::transparency render refused (short or bad dataset) — /transparency/ left as-is"
          else
            echo "::warning::no transparency dataset — /transparency/ left as-is"
          fi

      # After the transparency page, because the sitemap includes it.
      - name: Render pages
        run: node tools/render-pages.mjs

      - name: Commit + push if anything changed
        run: |
          git config user.name "saferoute-bot"
          git config user.email "minhaj@safe-route.app"
          git add -A
          git diff --cached --quiet && { echo "no changes"; exit 0; }
          git commit -m "safety pages: monthly data refresh"

          # This job runs for ~2 hours and main moves under it: run #6 fetched
          # all 20 cities and then lost the lot to a non-fast-forward push
          # because two cities landed while it ran.
          #
          # The rebase happens HERE, after the commit, so the tree is clean.
          # An earlier attempt rebased mid-run with --autostash and that is a
          # trap: when re-applying the stash conflicts, git prints "Applying
          # autostash resulted in conflicts", writes <<<<<<< markers into the
          # data files, and STILL EXITS 0. Run #7 went green on that step and
          # died in the renderer on a conflict marker inside a JSON file, after
          # 2h13m of fetching. Never autostash generated output.
          #
          # After a rebase, re-render: the pages and sitemap this run produced
          # do not know about a city that landed since, and ours would overwrite
          # theirs. render-pages.mjs skips a city with no data, so a half-added
          # city cannot break this.
          for i in 1 2 3; do
            if git push; then exit 0; fi
            echo "push rejected (attempt $i) — rebasing onto main"
            if ! git pull --rebase origin main; then
              git rebase --abort || true
              echo "::error::rebase conflicted with main — re-run the rebuild"
              exit 1
            fi
            node tools/render-pages.mjs
            git add -A
            git diff --cached --quiet || git commit --amend --no-edit
          done
          echo "::error::could not push after 3 attempts — main is moving faster than this job can rebase"
          exit 1
